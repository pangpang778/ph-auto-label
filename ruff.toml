target-version = "py312"
line-length = 120

[lint]
# Preserve the legacy lint baseline while Ruff is introduced. New checks can be
# enabled incrementally after the existing findings have been addressed.
select = ["E", "F"]
ignore = ["E302", "E303", "E305", "E402", "E501", "F401", "F841"]
