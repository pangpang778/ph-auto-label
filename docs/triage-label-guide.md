# Triage Label Guide

The triage workflow uses `bug` and `enhancement` as categories and one state label to describe the next action.

State labels are `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix`.
